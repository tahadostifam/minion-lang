// Veriable definition 
#a = 1;
#b = 2;

// Function definition
fn divide(num1, num2) {
    if num2 == 0 {
        throw "devidide by zero is not possible";
    }

    ret num1 / num2;
}

// Calling function
sum(a, b);


for #i = 0; i < 10; i++; {
    puts "i -> {i}";
}

// Array definition
#names = (
    "Taha"    |
    "Rust"    | 
    "Ruby"    |
    "Crystal" |
    "Go"      |
)

