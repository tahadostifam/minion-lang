fn fibonacci(n) {
    print("fibonacci->" + n);

    if (n <= 0) {
        ret 0;
    } else if (n == 1) {
        ret 1;
    } else {
        ret fibonacci(n - 1) + fibonacci(n - 2);
    }
}

#a = fibonacci(2);

print("Here is the Result: " + a);
print("Here is the Result: " + true);