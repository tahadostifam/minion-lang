// Variable definition
#a = 1;
#b = 2;

if (a == 2) {
    print(1);
}

// Function definition
fn divide(num1, num2) {
    if num2 == 0 {
        throw "devidide by zero is not possible";
    }

    ret num1 / num2;
}

// Calling function
divide(a, b);


// Array definition
#names = ["Taha", "Rust", "Ruby", "Go", "C#"];

for #i = 0; i < 10; i++ {
    println(i);
}

if (next() == 0) {
    print(10 / 2 * 3 + 1);
}

print("It works excellently!");
